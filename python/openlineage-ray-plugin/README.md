# OpenLineage example Ray plugin
Uses `setuptools` entrypoint to allow `pluggy` discover and load this plugin.

## Development
To build, first install `build`:
```bash
python -m pip install build
```
then:
```bash
python -m build .
```
